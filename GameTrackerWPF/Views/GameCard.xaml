<UserControl x:Class="GameTrackerWPF.Views.GameCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameTrackerWPF.Views"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             x:Name="Root"
             mc:Ignorable="d" d:DesignHeight="350" d:DesignWidth="200">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>

    <Border CornerRadius="4" 
            Background="#1E1E1E"
            BorderBrush="#373737"
            BorderThickness="1"
            DataContext="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition />
                <RowDefinition Height="30" />
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                   Text="{Binding Title, ElementName=Root}" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   TextTrimming="CharacterEllipsis"
                   ToolTip="{Binding Title, ElementName=Root}"/>

            <Border Grid.Row="1"
                Background="#121212"
                CornerRadius="4"
                Margin="0,10">
                <Border.OpacityMask>
                    <VisualBrush>
                        <VisualBrush.Visual>
                            <Border Background="#121212"
                                    CornerRadius="4"
                                    Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}"
                                    Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}"/>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Border.OpacityMask>
                <Image Source="{Binding CoverImage}" 
                       Stretch="UniformToFill" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>

            <TextBlock Grid.Row="2" Text="PLAY TIME"
                       HorizontalAlignment="Left" 
                       VerticalAlignment="Center"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center">
                <ui:SymbolIcon Symbol="XboxController32" 
                   HorizontalAlignment="Left"
                   FontSize="25" 
                   Margin="0,0,8,0"
                   Foreground="#22C55E"
                   Filled="True"
                   Visibility="{Binding IsRunning, ElementName=Root, Converter={StaticResource BoolToVisibility}}"/>

                <TextBlock Text="{Binding Playtime, ElementName=Root, Converter={StaticResource MinutesToTimeConverter}}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                    <Setter Property="Foreground" Value="#22C55E"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>

            <Grid Grid.Row="4" VerticalAlignment="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                        x:Name="btnEditGame"
                        Padding="0"
                        Height="32" 
                        Width="32"
                        Click="btnEditGame_Click">
                    <ui:SymbolIcon Symbol="Edit28" FontSize="18" Foreground="#FAFAFA"/>
                </Button>

                <Button Grid.Column="1" 
                    x:Name="btnRemoveGame"
                    Padding="0"
                    Height="32" 
                    Width="32"
                    Command="{Binding DataContext.RemoveGameCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                    CommandParameter="{Binding Id, ElementName=Root}">
                    <ui:SymbolIcon Symbol="Delete28" FontSize="18" Foreground="#FAFAFA"/>
                </Button>
            </Grid>

        </Grid>
    </Border>
</UserControl>
